import { Fragment } from "react";

const SkuSizeAndColor = ({ skuSizes, skuColors, skuInfo }) => {
  return (
    <Fragment>
      <div className="sizes-and-colors-container">size color</div>
      <div className="sku-info-container">info</div>
    </Fragment>
  );
};
export default SkuSizeAndColor;
